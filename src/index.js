import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App";

const base = document.getElementById("root");
const root = ReactDOM.createRoot(base);
root.render(<App />);
